// Think of something you could store in  type script objects. write a program that creates a list containing these items.
let car : {name:string,colour:string,model:number}={
    name:"Civic",
    colour:"White",
    model:2024,
};
console.log(`I have ${car.name} car model ${car.model} and colour is ${car.colour}. `) 